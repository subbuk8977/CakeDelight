apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: cake-delight

spec:
  selector:
    app: catalog-service

  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001

  type: ClusterIP