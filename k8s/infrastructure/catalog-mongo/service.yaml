apiVersion: v1
kind: Service
metadata:
  name: catalog-mongo
  namespace: cake-delight

spec:
  selector:
    app: catalog-mongo

  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017

  type: ClusterIP